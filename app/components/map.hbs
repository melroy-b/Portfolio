<LeafletMap @lat={{lat}} @lng={{lng}} @zoom={{zoom}} as |layers|>

    <layers.tile @url={{url}} />

  <layers.marker @location={{emberConfLocation}} as |marker|>
    <marker.popup>
      <h3>Don Bosco HS</h3>
      A vibrant school
    </marker.popup>
  </layers.marker>

</LeafletMap>